<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.myproject.mapper.StationMapper">

    <!--搜索车站（名称/城市/省份）  -->
    <select id="searchByKeyWord" resultType="com.myproject.entity.station">
        SELECT id, station_name, station_code, city, province, sort_order
        FROM station
        WHERE station_name LIKE CONCAT('%', #{keyword}, '%')
           OR city LIKE CONCAT('%', #{keyword}, '%')
           OR station_code LIKE CONCAT('%', #{keyword}, '%')
           OR province LIKE CONCAT('%', #{keyword}, '%')
        ORDER BY sort_order
    </select>

</mapper>